#!/usr/bin/env node
const app = require('../app');

try {
    const port = process.env.PORT
    app.listen(port);
    console.log('Microsservice login listening at http://localhost:%s', port);
} catch (e) {
    throw e;
}